import { AppendStringPipe } from './append-string.pipe';

describe('AppendStringPipe', () => {
  it('create an instance', () => {
    const pipe = new AppendStringPipe();
    expect(pipe).toBeTruthy();
  });
});
